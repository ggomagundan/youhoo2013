<%= form_for(@article) do |f| %>
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% @article.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%- f.label :title -%>
    <%= f.text_field :title, :style => "width:500px;", :placeholder =>"제목" %>
  </div>
  <div class="field">
    <%- f.label :content -%>
    <%= f.text_area :content, :size => "20x10", :placeholder =>"ex) 오늘 간 봉사활동 완전 보람있었어요 (140 제한)" , :maxlength => 140, :style => "width:800px;height:200px;" %>
  </div>
  <div class="field">
    <%- f.label :photo_id -%>
    <%= f.file_field :photo_id %>
  </div>
  <div class="actions ">
    <%= f.submit :class=>"btn btn-large btn-primary" %>
  </div>

<% end %>
